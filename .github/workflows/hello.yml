name: hello

on:
  push:
    branches: [main]

jobs:
  
  greet:
    runs-on: ubuntu-latest

    steps:

      - name: checkout code
        run: action/checkout@v4

      - name: say hello
        run: echo "Hello from github actions.!"
